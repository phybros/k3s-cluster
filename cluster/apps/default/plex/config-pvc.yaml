apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-config-v1
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rook-ceph-block
  resources:
    requests:
      storage: 40Gi
